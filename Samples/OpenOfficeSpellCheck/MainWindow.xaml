<Window x:Class="OpenOfficeSpellCheck.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenOfficeSpellCheck"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="SfSpellChecker - OpenOffice dictionary" Height="450" Width="800">
    <Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>
    <Grid VerticalAlignment="Center">
        <StackPanel>
            <TextBox Text="gracsq por venizr por favor ven de nuevoq" 
                 Height="300"
                 Width="300" 
                 x:Name="txtbx"
                 TextWrapping="Wrap"                
                 VerticalContentAlignment="Top"/>
            <Button HorizontalAlignment="Center"
                Content="Spell Check" 
                Command="{Binding Buttonclick}"
                CommandParameter="{Binding ElementName=txtbx}">
            </Button>
        </StackPanel>
    </Grid>
</Window>
